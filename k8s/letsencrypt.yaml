apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-prod
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: nurlykhank@gmail.com  # Replace with your email
    privateKeySecretRef:
      name: letsencrypt-progtexts2
    solvers:
    - http01:
        ingress:
          class: nginx